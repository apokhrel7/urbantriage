# urbantriage
An AI agent that listens to a 311 call, transcribes it, classifies the issue, scores its priority using an ML model trained on 6M real Calgary tickets, and instantly generates a complete dispatch-ready ticket for the operator to approve in the time it takes a human to pick up a pen.
